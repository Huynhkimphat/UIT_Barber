<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Mã HD</th>
            <th scope="col">KHUYENMAI</th>
            <th scope="col">TONGTIEN</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
        </tr>
    </thead>
    {{#each order}}
    <tbody>
        <tr>
            <th scope="row">{{MAHD}}</th>
            <td>{{KHUYENMAI}}</td>
            <td>{{TONGTIEN}}</td>
            <td>
                <a href="/order/{{MAHD}}/edit" class="btn btn-link">Edit</a>
            </td>
            <td>
                <a href=" " class="btn btn-link" data-bs-toggle="modal" data-bs-target="#deleteRecord"
                    data-id={{MADGSP}}>Delete
                </a>
            </td>
        </tr>
    </tbody>

    {{/each}}

</table>
<button class="btn btn-primary"><a href="/order/add">Add</a></button>
{{!-- Delete Model --}}
<div class="modal fade" id="deleteRecord" tabindex="-1" aria-labelledby="deleteRecordLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteRecordLabel">Delete productRating</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Do you want to delete productRating?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-bs-dismiss="modal">No</button>
                <button type="button" class="btn btn-warning" id="btn-delete-productRating">Yes</button>
            </div>
        </div>
    </div>
</div>
{{!-- Delete hidden Form --}}
<form name="delete-productRating-form" method="POST" action=""></form>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        let productRatingId;
        let deleteForm = document.forms['delete-productRating-form'];
        let btnDeleteProductRating = document.getElementById('btn-delete-productRating');

        // When dialog confirm clicked
        $('#deleteRecord').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget)
            productRatingId = button.data('id');
        });

        // When delete button clicked
        btnDeleteProductRating.onclick = function () {
            deleteForm.action = '/productRating/' + productRatingId + '?_method=DELETE';
            deleteForm.submit();
        };
    })
</script>